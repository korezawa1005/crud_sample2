<h1>詳細表示<h1>

<div>
  <p>お名前：<%= @user.name%></p>
  <p>年齢： <%= @user.age%></p>
</div>

<div>
  <p><%= link_to "一覧表示へ","/users" %></p>
  <p><%= link_to "編集", "/users/#{@user.id}/edit" %></p>
  <%= button_to "削除", "/users/#{@user.id}", data: {turbo: false} , method: :delete %>
</div>

